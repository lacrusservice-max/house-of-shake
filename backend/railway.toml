[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && npx prisma db seed || true && node src/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
